---
phase: 02-working-state-streaming-loop
plan: 06
type: execute
mode: gap_closure
wave: 6
depends_on:
  - "02-05"
files_modified:
  - package.json
  - src/app.tsx
  - src/components/Composer.tsx
  - src/config/env.ts
  - src/config/terminal-profile.ts
  - src/runtime/chat-session.ts
  - src/index.tsx
  - .env.example
  - README.md
  - tests/config/env.test.ts
  - tests/components/app-vscode-terminal-compat.test.tsx
  - tests/components/app-startup-health.test.tsx
  - .planning/phases/02-working-state-streaming-loop/02-UAT.md
requirements:
  - RELY-02
  - RELY-03
  - UX-TERM-01
  - CFG-LOCAL-01
autonomous: true
---

<objective>
收口 Phase 2 剩余的两个真实使用问题：VS Code Terminal 中文输入漂移，以及 DeepSeek 本地配置难以直接生效。
Purpose: 让用户在 VS Code Terminal 下至少拥有稳定可用的输入 fallback，同时让 DeepSeek 配置支持本地文件加载和明确诊断，不再依赖手工猜环境变量是否生效。
Output: VS Code fallback input 方案、本地 env 文件加载、启动配置诊断、更新后的文档和专项测试。
</objective>

<context>
@.planning/STATE.md
@.planning/phases/02-working-state-streaming-loop/02-UAT.md
@src/app.tsx
@src/components/Composer.tsx
@src/config/env.ts
@src/config/terminal-profile.ts
@README.md

Observed issues:
- VS Code Terminal 中，中文输入法候选框仍漂移
- 当前 shell 会话未读到 `DEEPSEEK_API_KEY`
- `.env.example` 当前包含真实样式 key，存在误导和安全风险
</context>

<must_haves>
- truth: 如果 VS Code Terminal 无法稳定承载自绘输入框，就必须提供更强的 fallback input 模式，而不是继续仅靠布局微调。
- truth: DeepSeek 配置必须支持本地文件加载，避免用户每次都手工 export。
- truth: 启动时的缺失配置提示必须告诉用户“去哪配、怎么配、当前用了哪种配置来源”。
</must_haves>

<tasks>

<task>
  <name>Task 1: 增加本地 env 文件加载与配置诊断</name>
  <files>src/config/env.ts, src/index.tsx, src/runtime/chat-session.ts, .env.example, README.md, tests/config/env.test.ts, tests/components/app-startup-health.test.tsx</files>
  <action>
    为 DeepSeek 配置增加本地文件 fallback，优先顺序至少明确为：
    1. 当前进程环境变量
    2. `.env.local`
    3. `.env.deepseek.local`
    更新 `.env.example` 为纯占位符，不再保留真实样式 key。
    在启动或首次请求前增加配置健康检查与诊断文案，明确告诉用户当前是否读到了 key、如果没读到应该把 key 放到哪里。
  </action>
  <verify>npm test -- tests/config/env.test.ts tests/components/app-startup-health.test.tsx</verify>
  <acceptance>
    - `src/config/env.ts` 支持本地 env 文件 fallback
    - `.env.example` 不再包含真实样式 key
    - 缺失配置时有明确修复建议
  </acceptance>
</task>

<task>
  <name>Task 2: 为 VS Code Terminal 设计更强的 fallback input 模式</name>
  <files>src/app.tsx, src/components/Composer.tsx, src/config/terminal-profile.ts, tests/components/app-vscode-terminal-compat.test.tsx</files>
  <action>
    将当前 VS Code compatibility mode 从“布局降级”升级为“输入机制降级”：
    - 评估并实现更稳定的 fallback input 模式，优先考虑避免高频自绘输入锚点
    - 兼容模式下允许牺牲部分视觉一致性，但必须保证普通提问、`/` 命令模式、`/exit` 和 `Esc` 中断可用
    - 在 UI 中明确告知当前处于 VS Code fallback input 模式
  </action>
  <verify>npm test -- tests/components/app-vscode-terminal-compat.test.tsx tests/components/app-command-mode.test.tsx tests/components/app-exit-and-input.test.tsx</verify>
  <acceptance>
    - VS Code Terminal 下进入更强的 fallback input 模式
    - 命令模式、退出与中断能力不回退
    - 界面中可见当前为 fallback 模式
  </acceptance>
</task>

<task>
  <name>Task 3: 补齐文档与真实复测口径</name>
  <files>README.md, .planning/phases/02-working-state-streaming-loop/02-UAT.md</files>
  <action>
    更新 README，明确写出：
    - 推荐的本地配置方式（`.env.local` / `.env.deepseek.local`）
    - VS Code Terminal 的 fallback input 模式说明
    - 真实复测步骤：中文输入法、命令模式、`/exit`、真实 DeepSeek 请求、`Esc` 中断
    更新 UAT gap 字段，使后续 `$gsd-verify-work` 可直接继续。
  </action>
  <verify>npm run build</verify>
  <acceptance>
    - README 明确本地配置文件方案
    - README 明确 VS Code fallback 模式
    - UAT 与本次计划一致
  </acceptance>
</task>

</tasks>

<verification>
- [ ] `npm test -- tests/config/env.test.ts tests/components/app-startup-health.test.tsx`
- [ ] `npm test -- tests/components/app-vscode-terminal-compat.test.tsx tests/components/app-command-mode.test.tsx tests/components/app-exit-and-input.test.tsx`
- [ ] `npm test -- tests/components/app-working-streaming.test.tsx tests/components/app-interrupt-errors.test.tsx`
- [ ] `npm run build`
- [ ] 用户在 VS Code Terminal 中完成中文输入与 DeepSeek 配置专项复测
</verification>

<success_criteria>
- DeepSeek 支持本地 env 文件加载并提供明确诊断
- `.env.example` 已去敏感化
- VS Code Terminal 下提供更强的 fallback input 模式
- Phase 2 的命令模式、退出、Working、Streaming 和中断能力保持可用
</success_criteria>

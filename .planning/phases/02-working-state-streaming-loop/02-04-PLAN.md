---
phase: 02-working-state-streaming-loop
plan: 04
type: execute
mode: gap_closure
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - package.json
  - src/app.tsx
  - src/components/Composer.tsx
  - src/commands/registry.ts
  - src/index.tsx
  - README.md
  - tests/components/composer.test.tsx
  - tests/components/app-command-mode.test.tsx
  - tests/components/app-interrupt-errors.test.tsx
  - tests/components/app-exit-and-input.test.tsx
autonomous: true
requirements:
  - EXEC-04
  - RELY-02
  - RELY-03
user_setup: []
must_haves:
  truths:
    - REPL 必须提供明确可发现的退出方式，而不是只能强行关闭终端
    - 真实终端输入时，输入框与输入法候选位置不能出现明显偏移
    - 中断、退出、提交三类按键行为不能互相打架
  artifacts:
    - path: "src/commands/registry.ts"
      provides: "可发现的退出命令入口"
      contains: "/exit"
    - path: "src/components/Composer.tsx"
      provides: "具备真实光标语义的输入组件"
      min_lines: 25
    - path: "tests/components/app-exit-and-input.test.tsx"
      provides: "退出命令和输入渲染回归测试"
      min_lines: 30
  key_links:
    - from: "src/app.tsx"
      to: "src/components/Composer.tsx"
      via: "受控输入与提交/退出路由"
      pattern: "onSubmit|onExit|value"
    - from: "src/commands/registry.ts"
      to: "README.md"
      via: "退出命令文档"
      pattern: "/exit"
---

<objective>
修复 Phase 2 UAT 暴露出的两个真实终端问题：缺少可发现的退出指令，以及手写输入框导致的光标/输入法候选位置偏移。  
Purpose: 把当前“能跑”的 REPL 提升为“在真实终端里可稳定使用”的 REPL，避免用户只能强退进程，也避免中文输入和长文本交互时出现错位。  
Output: 退出命令、改造后的输入组件、真实终端输入回归测试和更新后的本地说明。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-state-streaming-loop/02-UAT.md
@.planning/phases/02-working-state-streaming-loop/02-02-PLAN.md
@.planning/phases/02-working-state-streaming-loop/02-03-PLAN.md
@src/app.tsx
@src/components/Composer.tsx
@src/commands/registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加 REPL 退出命令与可发现提示</name>
  <files>src/commands/registry.ts, src/app.tsx, src/index.tsx, README.md, tests/components/app-exit-and-input.test.tsx</files>
  <read_first>src/commands/registry.ts, src/app.tsx, src/index.tsx, README.md, .planning/phases/02-working-state-streaming-loop/02-UAT.md</read_first>
  <action>在 `src/commands/registry.ts` 中新增 `/exit` 命令，描述明确写成“exit the repl session”。在 `src/app.tsx` 中加入显式退出路由：当输入 `/exit` 并提交时，不走模型请求，而是调用一个可注入的 `onExit` 回调；若未注入则调用 `process.exit(0)`。在 `src/index.tsx` 中把默认 `onExit` 透传给 `App`。在 `README.md` 中补充 `/exit` 的使用说明。创建 `tests/components/app-exit-and-input.test.tsx`，验证输入 `/exit` 后会触发退出回调，而不是调用 runtime.submit。</action>
  <verify>npm test -- tests/components/app-exit-and-input.test.tsx</verify>
  <acceptance_criteria>
    - src/commands/registry.ts contains "/exit"
    - src/commands/registry.ts contains "exit the repl session"
    - src/app.tsx contains "onExit"
    - README.md contains "/exit"
    - tests/components/app-exit-and-input.test.tsx contains "/exit"
  </acceptance_criteria>
  <done>REPL 具备明确、可发现、可测试的退出命令</done>
</task>

<task type="auto">
  <name>Task 2: 用光标感知输入组件替换当前手写 Composer</name>
  <files>package.json, src/components/Composer.tsx, src/app.tsx, tests/components/composer.test.tsx, tests/components/app-exit-and-input.test.tsx</files>
  <read_first>package.json, src/components/Composer.tsx, src/app.tsx, tests/components/composer.test.tsx, .planning/phases/02-working-state-streaming-loop/02-UAT.md</read_first>
  <action>在 `package.json` 中加入 `ink-text-input` 作为依赖。将 `src/components/Composer.tsx` 从“只渲染 Text 的假输入框”改为基于 `ink-text-input` 的受控输入组件，组件 props 至少包含 `value`、`placeholder`、`isDisabled`、`onChange`、`onSubmit`。在 `src/app.tsx` 中移除对普通字符、回车、退格的全局手写拼接逻辑，仅保留 `Esc` 中断与命令候选方向键控制，把文本输入、提交和编辑交给 `Composer`。更新 `tests/components/composer.test.tsx` 与 `tests/components/app-exit-and-input.test.tsx`，验证 Composer 占位提示仍存在、提交普通文本会进入 working、且输入逻辑不再依赖 `stdin.write` 的逐字符拼接副作用。</action>
  <verify>npm test -- tests/components/composer.test.tsx tests/components/app-exit-and-input.test.tsx tests/components/app-working-streaming.test.tsx</verify>
  <acceptance_criteria>
    - package.json contains "ink-text-input"
    - src/components/Composer.tsx contains "TextInput"
    - src/components/Composer.tsx contains "onSubmit"
    - src/app.tsx contains "onChange"
    - src/app.tsx does not contain "applyInput(`${uiState.draft}${input}`)"
  </acceptance_criteria>
  <done>输入框具有真实光标语义，真实终端和输入法候选位置不再依赖伪输入渲染</done>
</task>

<task type="auto">
  <name>Task 3: 补齐真实终端回归验证与说明</name>
  <files>README.md, tests/components/app-interrupt-errors.test.tsx, tests/components/app-working-streaming.test.tsx, .planning/phases/02-working-state-streaming-loop/02-UAT.md</files>
  <read_first>README.md, tests/components/app-interrupt-errors.test.tsx, tests/components/app-working-streaming.test.tsx, .planning/phases/02-working-state-streaming-loop/02-UAT.md</read_first>
  <action>在 `README.md` 的本地验收部分新增两条检查：一是使用 `/exit` 正常退出 REPL，二是在 Windows Terminal 或 VS Code Terminal 中输入中文时，输入法候选框应跟随输入框位置而不是漂移到右下角。补充或更新测试，确保退出命令不会破坏 `Esc` 中断逻辑，且 working / streaming 回归仍通过。执行结束后，把 `.planning/phases/02-working-state-streaming-loop/02-UAT.md` 中第 8 条 gap 对应的 root_cause 与 missing 字段补齐，说明根因是“手写伪输入框没有真实光标语义、缺少退出命令入口”。</action>
  <verify>npm test -- tests/components/app-interrupt-errors.test.tsx tests/components/app-working-streaming.test.tsx && npm run build</verify>
  <acceptance_criteria>
    - README.md contains "Windows Terminal"
    - README.md contains "输入法候选"
    - README.md contains "/exit"
    - .planning/phases/02-working-state-streaming-loop/02-UAT.md contains "root_cause:"
    - .planning/phases/02-working-state-streaming-loop/02-UAT.md contains "missing:"
  </acceptance_criteria>
  <done>退出能力、真实终端输入稳定性和本地验收说明形成闭环</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- tests/components/app-exit-and-input.test.tsx tests/components/composer.test.tsx tests/components/app-working-streaming.test.tsx tests/components/app-interrupt-errors.test.tsx` 可运行
- [ ] `npm run build` 成功
- [ ] README 明确包含 `/exit` 与输入法候选位置的本地复测步骤
</verification>

<success_criteria>
- 所有任务完成
- REPL 支持通过 `/exit` 正常退出
- 真实终端输入框不再依赖伪输入渲染，IME 候选位置偏移问题有对应修复路径与复测标准
</success_criteria>

<output>
After completion, update `.planning/phases/02-working-state-streaming-loop/02-UAT.md` and create `.planning/phases/02-working-state-streaming-loop/02-04-SUMMARY.md`
</output>

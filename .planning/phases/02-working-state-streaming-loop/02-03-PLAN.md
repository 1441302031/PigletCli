---
phase: 02-working-state-streaming-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app.tsx
  - src/runtime/chat-session.ts
  - src/components/Timeline.tsx
  - README.md
  - .env.example
  - tests/components/app-interrupt-errors.test.tsx
  - tests/runtime/chat-session.test.ts
autonomous: true
requirements:
  - EXEC-04
  - RELY-01
  - RELY-02
user_setup:
  - "本地自行配置 DEEPSEEK_API_KEY 后执行 README 中的验收步骤"
must_haves:
  truths:
    - Escape 可中断真实请求
    - 缺失 API Key 和请求失败会显示结构化反馈
    - 用户有清晰的本地启动与验收说明
  artifacts:
    - path: "README.md"
      provides: "本地配置与验收文档"
      contains: "DEEPSEEK_API_KEY"
    - path: ".env.example"
      provides: "示例环境变量"
      contains: "DEEPSEEK_API_KEY="
    - path: "src/runtime/chat-session.ts"
      provides: "中断与错误处理逻辑"
      min_lines: 40
  key_links:
    - from: "src/app.tsx"
      to: "src/runtime/chat-session.ts"
      via: "interrupt and error handoff"
      pattern: "Escape|interrupt|failed"
    - from: "README.md"
      to: ".env.example"
      via: "local setup instructions"
      pattern: "DEEPSEEK_API_KEY"
---

<objective>
补齐中断、结构化错误反馈与本地验收文档。
Purpose: 让 Phase 2 不只是能跑通 happy path，而是能在本地真正可用、可恢复、可验收。
Output: 中断链路、错误反馈、README / .env.example、本地验收测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md
@.planning/phases/02-working-state-streaming-loop/02-RESEARCH.md
@.planning/phases/02-working-state-streaming-loop/02-VALIDATION.md
@.planning/phases/02-working-state-streaming-loop/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 打通真实请求中断与恢复输入</name>
  <files>src/runtime/chat-session.ts, src/app.tsx, tests/runtime/chat-session.test.ts</files>
  <read_first>src/runtime/chat-session.ts, src/app.tsx, .planning/phases/02-working-state-streaming-loop/02-CONTEXT.md</read_first>
  <action>在 `src/runtime/chat-session.ts` 中通过 `AbortController` 或等效机制实现真实请求取消，并在 `src/app.tsx` 中把执行态下的 `Escape` 绑定到该取消逻辑。补充 `tests/runtime/chat-session.test.ts`，验证取消后状态进入 `interrupted`，并且会话可恢复到可再次提交的状态。</action>
  <verify>npm test -- tests/runtime/chat-session.test.ts</verify>
  <acceptance_criteria>
    - src/runtime/chat-session.ts contains "AbortController"
    - src/runtime/chat-session.ts contains "interrupted"
    - src/app.tsx contains "escape"
    - tests/runtime/chat-session.test.ts contains "interrupted"
  </acceptance_criteria>
  <done>真实请求取消与状态恢复逻辑存在</done>
</task>

<task type="auto">
  <name>Task 2: 增加结构化错误反馈与 App 级回归测试</name>
  <files>src/app.tsx, src/components/Timeline.tsx, tests/components/app-interrupt-errors.test.tsx</files>
  <read_first>src/app.tsx, src/components/Timeline.tsx, src/state/ui-machine.ts</read_first>
  <action>在 `src/app.tsx` 与 `src/components/Timeline.tsx` 中增加结构化状态反馈，至少覆盖缺失 `DEEPSEEK_API_KEY`、请求失败与用户中断三种情况。创建 `tests/components/app-interrupt-errors.test.tsx`，验证这三种状态下界面会显示明确提示，而不是空白或原始异常堆栈。</action>
  <verify>npm test -- tests/components/app-interrupt-errors.test.tsx</verify>
  <acceptance_criteria>
    - src/app.tsx contains "DEEPSEEK_API_KEY"
    - src/app.tsx contains "interrupted"
    - src/components/Timeline.tsx contains "error"
    - tests/components/app-interrupt-errors.test.tsx contains "DEEPSEEK_API_KEY"
  </acceptance_criteria>
  <done>结构化错误与中断反馈闭环完成</done>
</task>

<task type="auto">
  <name>Task 3: 补充本地配置与验收文档</name>
  <files>README.md, .env.example</files>
  <read_first>.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md, .planning/phases/02-working-state-streaming-loop/02-RESEARCH.md, package.json</read_first>
  <action>创建或更新 `README.md`，明确写出本地启动步骤：安装依赖、设置 `DEEPSEEK_API_KEY`、运行 `npm run dev`、使用 `/model` 切换 DeepSeek 模型、发送消息验证真实流式返回。创建 `.env.example`，至少包含 `DEEPSEEK_API_KEY=` 占位，不写入任何真实值。文档中要明确说明真实密钥不得提交到仓库。</action>
  <verify>检查 README 与 .env.example 中的本地配置步骤完整</verify>
  <acceptance_criteria>
    - README.md contains "DEEPSEEK_API_KEY"
    - README.md contains "npm run dev"
    - README.md contains "/model"
    - .env.example contains "DEEPSEEK_API_KEY="
  </acceptance_criteria>
  <done>本地配置与真实验收路径文档化完成</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- tests/runtime/chat-session.test.ts tests/components/app-interrupt-errors.test.tsx` 可运行
- [ ] 缺失密钥、请求失败与中断都有明确反馈
- [ ] README 可指导用户完成一次真实本地 DeepSeek 对话验收
</verification>

<success_criteria>
- 所有任务完成
- Phase 2 的真实执行链路具备中断、错误与本地验收能力
- 用户可按文档独立完成本地 DeepSeek 交互验证
</success_criteria>

<output>
After completion, create `.planning/phases/02-working-state-streaming-loop/02-03-SUMMARY.md`
</output>

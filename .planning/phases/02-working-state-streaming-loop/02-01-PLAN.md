---
phase: 02-working-state-streaming-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - src/index.tsx
  - src/state/ui-machine.ts
  - src/state/session-store.ts
  - src/commands/registry.ts
  - src/config/env.ts
  - src/providers/deepseek/client.ts
  - tests/state/ui-machine.test.ts
  - tests/providers/deepseek-client.test.ts
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
user_setup:
  - "在本地环境中设置 DEEPSEEK_API_KEY，禁止把真实密钥写入仓库文件"
must_haves:
  truths:
    - 提交消息后应用具备明确 execution 生命周期，而不是停留在静态输入壳层
    - DeepSeek API Key 通过本地环境变量读取，缺失时有明确提示
    - DeepSeek client 与 UI 组件边界解耦，可被后续流式 UI 复用
  artifacts:
    - path: "src/config/env.ts"
      provides: "本地环境变量读取与错误提示"
      min_lines: 20
    - path: "src/providers/deepseek/client.ts"
      provides: "DeepSeek 请求 client"
      min_lines: 25
    - path: "src/state/ui-machine.ts"
      provides: "扩展后的执行态状态机"
      min_lines: 30
  key_links:
    - from: "src/index.tsx"
      to: "src/config/env.ts"
      via: "boot model / config loading"
      pattern: "DEEPSEEK_API_KEY|env"
    - from: "src/providers/deepseek/client.ts"
      to: "src/state/ui-machine.ts"
      via: "request lifecycle handoff"
      pattern: "working|streaming|Abort"
---

<objective>
建立 DeepSeek 的本地配置与执行生命周期基础设施。
Purpose: 先把真实 provider 接入边界、环境变量读取和 execution 状态打好，避免后续 working / streaming UI 写成耦合逻辑。
Output: 环境变量模块、DeepSeek client、扩展后的状态机、模型注册与对应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md
@.planning/phases/02-working-state-streaming-loop/02-RESEARCH.md
@.planning/phases/02-working-state-streaming-loop/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加本地环境变量与 DeepSeek provider 基础模块</name>
  <files>package.json, .gitignore, src/config/env.ts, src/providers/deepseek/client.ts, tests/providers/deepseek-client.test.ts</files>
  <read_first>.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md, .planning/phases/02-working-state-streaming-loop/02-RESEARCH.md, package.json</read_first>
  <action>在 `package.json` 中补充运行时所需依赖；若采用 `.env.local` 或 `.env` 读取方案，则把该文件名加入 `.gitignore`。创建 `src/config/env.ts`，显式读取 `DEEPSEEK_API_KEY`，并导出缺失配置时可复用的错误信息。创建 `src/providers/deepseek/client.ts`，封装 DeepSeek 基础请求参数，支持模型名 `deepseek-chat` 与 `deepseek-reasoner`，并为后续流式请求保留 `AbortSignal` 参数。创建 `tests/providers/deepseek-client.test.ts`，验证 client 会发送 DeepSeek 模型名并在缺失密钥时返回明确错误。</action>
  <verify>npm test -- tests/providers/deepseek-client.test.ts</verify>
  <acceptance_criteria>
    - src/config/env.ts contains "DEEPSEEK_API_KEY"
    - src/providers/deepseek/client.ts contains "deepseek-chat"
    - src/providers/deepseek/client.ts contains "deepseek-reasoner"
    - src/providers/deepseek/client.ts contains "AbortSignal"
    - tests/providers/deepseek-client.test.ts contains "DEEPSEEK_API_KEY"
  </acceptance_criteria>
  <done>本地配置读取与 DeepSeek provider 基础模块存在且可测试</done>
</task>

<task type="auto">
  <name>Task 2: 扩展状态机与会话状态以承载执行态</name>
  <files>src/state/ui-machine.ts, src/state/session-store.ts, tests/state/ui-machine.test.ts</files>
  <read_first>src/state/ui-machine.ts, src/state/session-store.ts, .planning/phases/02-working-state-streaming-loop/02-CONTEXT.md</read_first>
  <action>在 `src/state/ui-machine.ts` 中新增 `working`、`streaming`、`interrupted`、`failed` 等模式，并显式补充提交开始、流式开始、流式完成、中断、失败等事件。更新 `src/state/session-store.ts`，加入当前模型、正在流式输出的消息、等待开始时间或等效 execution 元数据。扩展 `tests/state/ui-machine.test.ts`，至少覆盖从提交进入 `working`、收到首 chunk 进入 `streaming`、取消进入 `interrupted`、异常进入 `failed` 的状态切换。</action>
  <verify>npm test -- tests/state/ui-machine.test.ts</verify>
  <acceptance_criteria>
    - src/state/ui-machine.ts contains "working"
    - src/state/ui-machine.ts contains "streaming"
    - src/state/ui-machine.ts contains "interrupted"
    - src/state/ui-machine.ts contains "failed"
    - src/state/session-store.ts contains "model"
  </acceptance_criteria>
  <done>执行态状态机与会话容器具备 provider 接入所需的最小状态</done>
</task>

<task type="auto">
  <name>Task 3: 让命令注册表承载真实模型选项</name>
  <files>src/commands/registry.ts, src/index.tsx</files>
  <read_first>src/commands/registry.ts, src/index.tsx, .planning/phases/02-working-state-streaming-loop/02-CONTEXT.md</read_first>
  <action>更新 `src/commands/registry.ts`，把 `/model` 的描述改为与真实模型切换相符，并确保后续可以挂接 `deepseek-chat` 与 `deepseek-reasoner`。更新 `src/index.tsx` 的 boot model 注入逻辑，使默认模型名不再硬编码为 GPT，而是与 Phase 2 的默认模型 `deepseek-chat` 对齐或改为从会话状态读取。</action>
  <verify>检查 boot 信息与命令注册表中的模型文案一致</verify>
  <acceptance_criteria>
    - src/commands/registry.ts contains "/model"
    - src/commands/registry.ts contains "model"
    - src/index.tsx contains "deepseek-chat"
  </acceptance_criteria>
  <done>模型命令与启动默认模型对齐到真实 DeepSeek 方案</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- tests/providers/deepseek-client.test.ts tests/state/ui-machine.test.ts` 可运行
- [ ] DeepSeek 基础配置读取存在且缺失密钥时有明确提示
- [ ] 状态机具备 working / streaming / interrupted / failed 扩展
</verification>

<success_criteria>
- 所有任务完成
- DeepSeek provider 边界建立完成
- 本地配置与执行生命周期基础设施就绪
</success_criteria>

<output>
After completion, create `.planning/phases/02-working-state-streaming-loop/02-01-SUMMARY.md`
</output>

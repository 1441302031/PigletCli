---
phase: 02-working-state-streaming-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app.tsx
  - src/components/Composer.tsx
  - src/components/WorkingIndicator.tsx
  - src/components/Timeline.tsx
  - src/runtime/chat-session.ts
  - tests/components/app-working-streaming.test.tsx
  - tests/components/working-indicator.test.tsx
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - TIME-02
user_setup: []
must_haves:
  truths:
    - 用户提交后 100ms 内看到 working 反馈
    - waiting 时间可见并递增
    - DeepSeek 回复以流式方式进入时间线而不是整块回填
  artifacts:
    - path: "src/components/WorkingIndicator.tsx"
      provides: "working 指示器与计时展示"
      min_lines: 20
    - path: "src/runtime/chat-session.ts"
      provides: "提交到流式回写的会话控制"
      min_lines: 30
    - path: "src/components/Timeline.tsx"
      provides: "时间线渲染"
      min_lines: 20
  key_links:
    - from: "src/app.tsx"
      to: "src/runtime/chat-session.ts"
      via: "submit and stream integration"
      pattern: "stream|submit|working"
    - from: "src/app.tsx"
      to: "src/components/WorkingIndicator.tsx"
      via: "execution state rendering"
      pattern: "WorkingIndicator"
---

<objective>
将 DeepSeek 真实调用链接入 UI，完成 working 提示、等待时长与流式输出闭环。
Purpose: 让 Phase 1 的静态 REPL 壳层真正“活起来”，用户提交后立即看到系统正在工作，并逐步收到真实返回。
Output: Working 指示器、时间线渲染、流式会话控制、App 级集成与对应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md
@.planning/phases/02-working-state-streaming-loop/02-RESEARCH.md
@.planning/phases/02-working-state-streaming-loop/02-VALIDATION.md
@.planning/phases/02-working-state-streaming-loop/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 working 指示器与等待时长展示</name>
  <files>src/components/WorkingIndicator.tsx, tests/components/working-indicator.test.tsx</files>
  <read_first>.planning/phases/02-working-state-streaming-loop/02-CONTEXT.md, src/theme/tokens.ts, src/app.tsx</read_first>
  <action>创建 `src/components/WorkingIndicator.tsx`，渲染 `Working` 文案、等待秒数和中断提示，至少包含 `Esc to interrupt`。组件需要支持灰色到高光的循环样式状态或等效可测试的高亮切换标识。创建 `tests/components/working-indicator.test.tsx`，验证组件会显示 `Working`、等待秒数和中断提示。</action>
  <verify>npm test -- tests/components/working-indicator.test.tsx</verify>
  <acceptance_criteria>
    - src/components/WorkingIndicator.tsx contains "Working"
    - src/components/WorkingIndicator.tsx contains "Esc"
    - tests/components/working-indicator.test.tsx contains "Working"
  </acceptance_criteria>
  <done>working 指示器组件与测试存在</done>
</task>

<task type="auto">
  <name>Task 2: 增加流式会话控制与时间线渲染</name>
  <files>src/runtime/chat-session.ts, src/components/Timeline.tsx, src/state/session-store.ts</files>
  <read_first>src/state/session-store.ts, src/providers/deepseek/client.ts, .planning/phases/02-working-state-streaming-loop/02-RESEARCH.md</read_first>
  <action>创建 `src/runtime/chat-session.ts`，负责把用户提交转成 DeepSeek 请求、按流式 chunk 累积助手消息并写回 session 状态。创建 `src/components/Timeline.tsx`，渲染用户消息、助手消息与进行中的流式消息。必要时更新 `src/state/session-store.ts`，确保时间线 item 至少区分 user、assistant、status 三类。</action>
  <verify>检查 runtime 与 timeline 之间存在显式数据传递关系</verify>
  <acceptance_criteria>
    - src/runtime/chat-session.ts contains "stream"
    - src/runtime/chat-session.ts contains "deepseek"
    - src/components/Timeline.tsx contains "assistant"
    - src/components/Timeline.tsx contains "user"
  </acceptance_criteria>
  <done>真实流式消息可被会话层累积并进入时间线渲染</done>
</task>

<task type="auto">
  <name>Task 3: 将 working / streaming / model 选择接入 App</name>
  <files>src/app.tsx, src/components/Composer.tsx, tests/components/app-working-streaming.test.tsx</files>
  <read_first>src/app.tsx, src/components/Composer.tsx, src/runtime/chat-session.ts, src/commands/registry.ts</read_first>
  <action>更新 `src/app.tsx`，在提交消息后调用会话控制层，立即显示 `WorkingIndicator`，并把流式输出通过 `Timeline` 渲染出来。更新 `Composer` 或相关命令处理逻辑，使 `/model` 能切换到 `deepseek-chat` 与 `deepseek-reasoner`。创建 `tests/components/app-working-streaming.test.tsx`，验证提交后出现 `Working`，并在模拟流式返回时逐步出现助手内容。</action>
  <verify>npm test -- tests/components/app-working-streaming.test.tsx</verify>
  <acceptance_criteria>
    - src/app.tsx contains "WorkingIndicator"
    - src/app.tsx contains "Timeline"
    - src/app.tsx contains "deepseek-chat"
    - src/app.tsx contains "deepseek-reasoner"
    - tests/components/app-working-streaming.test.tsx contains "Working"
  </acceptance_criteria>
  <done>App 级 working / streaming / model 选择闭环建立完成</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- tests/components/working-indicator.test.tsx tests/components/app-working-streaming.test.tsx` 可运行
- [ ] 提交消息后显示 Working 与等待时长
- [ ] 流式消息逐步出现在时间线
</verification>

<success_criteria>
- 所有任务完成
- working / streaming UI 已接入真实会话链路
- 用户能在 CLI 中切换 DeepSeek 模型并看到真实流式回复
</success_criteria>

<output>
After completion, create `.planning/phases/02-working-state-streaming-loop/02-02-SUMMARY.md`
</output>

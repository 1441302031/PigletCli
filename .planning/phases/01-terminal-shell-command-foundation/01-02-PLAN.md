---
phase: 01-terminal-shell-command-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/state/ui-machine.ts
  - src/state/session-store.ts
  - src/components/Composer.tsx
  - src/app.tsx
  - tests/state/ui-machine.test.ts
autonomous: true
requirements:
  - SHEL-03
user_setup: []
must_haves:
  truths:
    - 用户始终拥有固定在底部的输入区
    - 根应用具备基础 UI 模式切换能力
    - 会话壳层可以承载后续命令面板与时间线状态
  artifacts:
    - path: "src/state/ui-machine.ts"
      provides: "UI 状态机"
      min_lines: 20
    - path: "src/state/session-store.ts"
      provides: "会话状态容器"
      min_lines: 20
    - path: "src/components/Composer.tsx"
      provides: "底部输入区"
      min_lines: 20
  key_links:
    - from: "src/app.tsx"
      to: "src/state/ui-machine.ts"
      via: "mode state management"
      pattern: "ui-machine|transition|mode"
    - from: "src/app.tsx"
      to: "src/components/Composer.tsx"
      via: "component composition"
      pattern: "Composer"
---

<objective>
实现底部输入区和基础状态机。

Purpose: 为命令模式、working 状态和流式输出建立最小可扩展的 UI 生命周期边界。
Output: Composer 组件、UI 状态机、会话状态容器及对应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md
@.planning/phases/01-terminal-shell-command-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立基础 UI 状态机</name>
  <files>src/state/ui-machine.ts, tests/state/ui-machine.test.ts</files>
  <read_first>.planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md, docs/superpowers/plans/2026-03-30-local-repl-agent-ui.md</read_first>
  <action>创建 `src/state/ui-machine.ts`，至少定义 `idle`、`command_suggesting`、`submitting`、`completed` 状态及输入变化、提交、命令退出等事件转换。创建 `tests/state/ui-machine.test.ts`，验证输入 `/` 后进入 `command_suggesting`，以及从提交回到完成态的基础切换。</action>
  <verify>npm test -- --runInBand</verify>
  <acceptance_criteria>
    - src/state/ui-machine.ts contains "command_suggesting"
    - src/state/ui-machine.ts contains "submitting"
    - tests/state/ui-machine.test.ts contains "INPUT_CHANGED"
    - tests/state/ui-machine.test.ts contains "command_suggesting"
  </acceptance_criteria>
  <done>状态机和核心切换断言存在</done>
</task>

<task type="auto">
  <name>Task 2: 实现底部输入区与会话状态容器</name>
  <files>src/state/session-store.ts, src/components/Composer.tsx, src/app.tsx</files>
  <read_first>src/app.tsx, .planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>创建 `src/state/session-store.ts` 作为最小会话状态容器，持有当前 draft、timeline 占位和 mode。创建 `src/components/Composer.tsx`，以受控方式渲染底部输入区，默认单行输入，`Enter` 提交。更新 `src/app.tsx`，把 Composer 固定在底部布局中，并与 UI 状态机关联。</action>
  <verify>检查 App、Composer 和状态模块之间的连接</verify>
  <acceptance_criteria>
    - src/components/Composer.tsx contains "Enter"
    - src/app.tsx contains "Composer"
    - src/state/session-store.ts contains "draft"
    - src/state/session-store.ts contains "timeline"
  </acceptance_criteria>
  <done>底部输入区固定存在并与状态容器打通</done>
</task>

<task type="auto">
  <name>Task 3: 保证输入区不被时间线布局挤走</name>
  <files>src/app.tsx, src/components/Composer.tsx</files>
  <read_first>src/app.tsx, src/components/Composer.tsx, docs/superpowers/specs/2026-03-30-local-repl-agent-ui-design.md</read_first>
  <action>调整根布局，使时间线区域与底部输入区分离。即使时间线后续变长，Composer 也必须留在固定底部区域。用清晰的布局注释或结构划分区分欢迎区、时间线区、输入区。</action>
  <verify>检查 App 布局中存在独立输入区容器</verify>
  <acceptance_criteria>
    - src/app.tsx contains "timeline"
    - src/app.tsx contains "composer"
    - src/app.tsx contains "WelcomeCard"
  </acceptance_criteria>
  <done>底部输入区在布局结构上被固定为独立区域</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --runInBand` 可运行
- [ ] 状态机能进入命令模式
- [ ] Composer 在根布局中是独立底部区域
</verification>

<success_criteria>

- 所有任务完成
- 底部输入区与状态机已就位
- Phase 3 之前不需要重写输入基础结构
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-shell-command-foundation/01-02-SUMMARY.md`
</output>


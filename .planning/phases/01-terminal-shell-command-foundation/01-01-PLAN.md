---
phase: 01-terminal-shell-command-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/index.tsx
  - src/app.tsx
  - src/theme/tokens.ts
  - src/components/WelcomeCard.tsx
autonomous: true
requirements:
  - SHEL-01
  - SHEL-02
user_setup: []
must_haves:
  truths:
    - 用户启动 CLI 后可以看到欢迎卡片
    - 欢迎卡片显示产品名、版本、当前模型与目录
    - 底部输入区在初始壳层中已经固定可见
  artifacts:
    - path: "src/index.tsx"
      provides: "CLI 启动入口"
      min_lines: 10
    - path: "src/app.tsx"
      provides: "根应用壳层"
      min_lines: 20
    - path: "src/components/WelcomeCard.tsx"
      provides: "欢迎卡片渲染"
      min_lines: 20
    - path: "package.json"
      provides: "脚本与依赖定义"
      contains: "\"build\""
  key_links:
    - from: "src/index.tsx"
      to: "src/app.tsx"
      via: "Ink render"
      pattern: "render\\(|<App"
    - from: "src/app.tsx"
      to: "src/components/WelcomeCard.tsx"
      via: "component composition"
      pattern: "WelcomeCard"
---

<objective>
建立终端应用壳层和欢迎区基础骨架。

Purpose: 先交付一个看起来已经像正式 Agent CLI 的启动界面，为后续输入、命令和流式交互提供稳定容器。
Output: CLI 入口、根应用、欢迎卡片、基础主题 token、测试与构建脚本。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md
@.planning/phases/01-terminal-shell-command-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 CLI 项目脚手架与测试基础</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-terminal-shell-command-foundation/01-RESEARCH.md, docs/superpowers/plans/2026-03-30-local-repl-agent-ui.md</read_first>
  <action>创建 Node.js + TypeScript + Ink 项目基础配置。package.json 至少定义 scripts: "build"="tsc -p tsconfig.json"、"dev"="tsx src/index.tsx"、"test"="vitest run"；添加 `ink`、`react` 以及测试依赖。tsconfig.json 配置适用于 Node ESM 的 TypeScript 编译。vitest.config.ts 建立基础测试运行配置。</action>
  <verify>检查 `package.json`、`tsconfig.json`、`vitest.config.ts` 已创建并包含约定脚本</verify>
  <acceptance_criteria>
    - package.json contains "\"build\": \"tsc -p tsconfig.json\""
    - package.json contains "\"dev\": \"tsx src/index.tsx\""
    - package.json contains "\"test\": \"vitest run\""
    - tsconfig.json contains "\"compilerOptions\""
    - vitest.config.ts contains "defineConfig"
  </acceptance_criteria>
  <done>CLI 与测试基础设施文件存在且脚本名称正确</done>
</task>

<task type="auto">
  <name>Task 2: 实现根应用壳层与欢迎卡片</name>
  <files>src/index.tsx, src/app.tsx, src/theme/tokens.ts, src/components/WelcomeCard.tsx</files>
  <read_first>docs/superpowers/specs/2026-03-30-local-repl-agent-ui-design.md, .planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>创建 `src/index.tsx` 作为 Ink render 入口，创建 `src/app.tsx` 组合 WelcomeCard、时间线占位区和底部输入区占位容器。创建 `src/theme/tokens.ts` 定义深色终端风格的基础颜色 token。创建 `src/components/WelcomeCard.tsx`，显示产品名 `PigLet CLI`、版本、当前模型、当前目录和一条中文提示文案。</action>
  <verify>检查欢迎卡片组件和 App 组合关系存在</verify>
  <acceptance_criteria>
    - src/index.tsx contains "render("
    - src/app.tsx contains "WelcomeCard"
    - src/components/WelcomeCard.tsx contains "PigLet CLI"
    - src/components/WelcomeCard.tsx contains "model"
    - src/components/WelcomeCard.tsx contains "directory"
  </acceptance_criteria>
  <done>启动壳层和欢迎卡片结构完成</done>
</task>

<task type="auto">
  <name>Task 3: 为欢迎壳层补充最小 smoke 测试</name>
  <files>tests/components/welcome-card.test.tsx</files>
  <read_first>src/app.tsx, src/components/WelcomeCard.tsx, .planning/phases/01-terminal-shell-command-foundation/01-VALIDATION.md</read_first>
  <action>创建 `tests/components/welcome-card.test.tsx`，使用 Ink 测试库断言欢迎卡片包含产品名、模型与目录。测试名称需直接表达壳层目标，例如 `renders welcome shell with product metadata`。</action>
  <verify>npm test -- --runInBand</verify>
  <acceptance_criteria>
    - tests/components/welcome-card.test.tsx contains "PigLet CLI"
    - tests/components/welcome-card.test.tsx contains "renders welcome shell"
  </acceptance_criteria>
  <done>欢迎卡片有自动化 smoke 测试入口</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --runInBand` 可运行
- [ ] `npm run build` 可运行
- [ ] 欢迎卡片显示产品元信息
</verification>

<success_criteria>

- 所有任务完成
- CLI 项目骨架存在
- 欢迎卡片能承载产品信息
- 后续输入区与命令模式有可复用的应用容器
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-shell-command-foundation/01-01-SUMMARY.md`
</output>


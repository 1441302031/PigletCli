---
phase: 01-terminal-shell-command-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/commands/types.ts
  - src/commands/registry.ts
  - src/commands/filter.ts
  - src/components/CommandPalette.tsx
  - src/components/Composer.tsx
  - src/app.tsx
  - tests/commands/filter.test.ts
  - tests/components/command-palette.test.tsx
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
user_setup: []
must_haves:
  truths:
    - 用户输入 `/` 后可以看到命令候选列表
    - 用户可以使用方向键和回车操作命令候选
    - 用户可以用 `Tab` 补全和 `Esc` 退出命令模式
  artifacts:
    - path: "src/commands/registry.ts"
      provides: "命令注册表"
      min_lines: 20
    - path: "src/commands/filter.ts"
      provides: "过滤与排序逻辑"
      min_lines: 20
    - path: "src/components/CommandPalette.tsx"
      provides: "命令候选面板"
      min_lines: 25
  key_links:
    - from: "src/components/Composer.tsx"
      to: "src/components/CommandPalette.tsx"
      via: "slash mode rendering"
      pattern: "CommandPalette"
    - from: "src/commands/filter.ts"
      to: "src/commands/registry.ts"
      via: "command ranking"
      pattern: "filter|sort|match"
---

<objective>
实现命令注册、过滤排序和命令候选面板。

Purpose: 让 `/` 真正成为可发现、可操作的能力入口，而不是只能靠记忆输入的隐藏功能。
Output: 命令注册表、过滤器、命令候选面板、Composer 集成和测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md
@.planning/phases/01-terminal-shell-command-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建命令注册表与过滤排序逻辑</name>
  <files>src/commands/types.ts, src/commands/registry.ts, src/commands/filter.ts, tests/commands/filter.test.ts</files>
  <read_first>.planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md, .planning/REQUIREMENTS.md, docs/superpowers/plans/2026-03-30-local-repl-agent-ui.md</read_first>
  <action>创建命令类型定义、内建命令注册表和过滤排序逻辑。注册表至少包含 `/model`、`/fast`、`/permissions`、`/review`、`/new`。过滤逻辑必须实现“精确前缀 > 别名 > 描述关键词 > 字母序”规则。创建 `tests/commands/filter.test.ts` 覆盖排序和别名匹配。</action>
  <verify>npm test -- --runInBand</verify>
  <acceptance_criteria>
    - src/commands/registry.ts contains "/model"
    - src/commands/registry.ts contains "/permissions"
    - src/commands/filter.ts contains "prefix"
    - tests/commands/filter.test.ts contains "/model"
    - tests/commands/filter.test.ts contains "aliases"
  </acceptance_criteria>
  <done>命令数据与过滤规则可独立测试</done>
</task>

<task type="auto">
  <name>Task 2: 实现命令候选面板组件</name>
  <files>src/components/CommandPalette.tsx, tests/components/command-palette.test.tsx</files>
  <read_first>src/commands/registry.ts, src/commands/filter.ts, .planning/phases/01-terminal-shell-command-foundation/01-CONTEXT.md</read_first>
  <action>创建 `src/components/CommandPalette.tsx`，渲染命令名、说明文本和高亮项；无匹配时显示中文空状态提示。创建组件测试，断言候选面板能显示命令说明，并在无结果时显示提示。</action>
  <verify>npm test -- --runInBand</verify>
  <acceptance_criteria>
    - src/components/CommandPalette.tsx contains "No matching command"
    - src/components/CommandPalette.tsx contains "description"
    - tests/components/command-palette.test.tsx contains "No matching command"
  </acceptance_criteria>
  <done>命令候选面板具备渲染和空态能力</done>
</task>

<task type="auto">
  <name>Task 3: 将命令模式接入 Composer 与 App</name>
  <files>src/components/Composer.tsx, src/app.tsx</files>
  <read_first>src/components/Composer.tsx, src/components/CommandPalette.tsx, src/state/ui-machine.ts, src/commands/filter.ts</read_first>
  <action>更新 Composer 与 App：当输入以 `/` 开头时进入 `command_suggesting`，展示 CommandPalette；实现 `Up`、`Down`、`Enter`、`Tab`、`Esc` 对命令模式的控制；默认高亮当前最佳匹配项。命令面板必须位于输入区附近，且不影响底部输入区固定布局。</action>
  <verify>检查 Composer 和 App 中存在命令模式集成逻辑</verify>
  <acceptance_criteria>
    - src/components/Composer.tsx contains "Tab"
    - src/components/Composer.tsx contains "Escape"
    - src/components/Composer.tsx contains "command_suggesting"
    - src/app.tsx contains "CommandPalette"
  </acceptance_criteria>
  <done>命令模式从输入到候选渲染再到键盘选择完成闭环</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --runInBand` 可运行
- [ ] 输入 `/` 后会展示候选面板
- [ ] 方向键、回车、Tab、Esc 能控制命令模式
</verification>

<success_criteria>

- 所有任务完成
- 所有命令相关需求在本阶段都有明确执行路径
- 用户不查文档也能通过 `/` 发现命令能力
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-shell-command-foundation/01-03-SUMMARY.md`
</output>


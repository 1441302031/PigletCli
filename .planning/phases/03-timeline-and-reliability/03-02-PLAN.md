---
phase: 03-timeline-and-reliability
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/app.tsx
  - src/runtime/chat-session.ts
  - src/components/Timeline.tsx
  - tests/components/app-structured-errors.test.tsx
autonomous: true
requirements:
  - RELY-01
  - TIME-03
must_haves:
  truths:
    - 所有失败路径都应落到统一的错误/状态映射规则
    - 错误反馈必须包含恢复建议，而不是只输出错误字符串
    - 中断、失败、缺失配置至少要在时间线层面可区分
---

<objective>
实现结构化错误块与请求状态事件。Purpose: 把当前散落的失败提示和中断反馈收敛成统一、可读、可恢复的时间线输出。Output: 错误块映射逻辑、状态事件写入逻辑和对应自动化测试。
</objective>

<execution_context>
@.planning/phases/03-timeline-and-reliability/03-CONTEXT.md
@.planning/phases/03-timeline-and-reliability/03-RESEARCH.md
@.planning/phases/03-timeline-and-reliability/03-VALIDATION.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 定义错误块与状态事件映射</name>
  <files>src/app.tsx, src/runtime/chat-session.ts</files>
  <action>把请求开始、失败、中断、完成映射为稳定的时间线 item；为缺失配置、网络失败、用户中断等场景生成结构化错误块或状态事件。</action>
  <verify>检查失败和中断路径不再只追加裸字符串</verify>
  <done>请求生命周期已有统一事件映射</done>
</task>

<task type="auto">
  <name>Task 2: 在 Timeline 中渲染结构化错误块</name>
  <files>src/components/Timeline.tsx, tests/components/app-structured-errors.test.tsx</files>
  <action>为 `error_block` 提供可读的终端样式，至少包含标题、摘要和恢复建议，并补充测试覆盖缺失 key、网络失败和用户中断的展示结果。</action>
  <verify>npm test -- tests/components/app-structured-errors.test.tsx</verify>
  <done>错误反馈完成从裸文本到结构化时间线块的升级</done>
</task>

</tasks>

<verification>
- [ ] 错误块包含恢复建议
- [ ] 中断、失败、缺失配置在时间线中可区分
- [ ] `npm test -- tests/components/app-structured-errors.test.tsx` 可通过
</verification>

<success_criteria>
- 失败路径统一且可恢复
- 结构化事件已落到时间线
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-and-reliability/03-02-SUMMARY.md`
</output>

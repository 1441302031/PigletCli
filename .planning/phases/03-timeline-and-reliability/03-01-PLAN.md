---
phase: 03-timeline-and-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/session-store.ts
  - src/components/Timeline.tsx
  - src/app.tsx
  - tests/components/timeline.test.tsx
  - tests/components/app-timeline-events.test.tsx
autonomous: true
requirements:
  - TIME-01
  - TIME-03
must_haves:
  truths:
    - 时间线必须继续是唯一主输出区域，而不是新增第二套结果面板
    - timeline item 结构必须能表达消息、状态、错误和完成事件
    - 命令反馈与普通对话反馈要能共用同一时间线模型
  artifacts:
    - path: "src/state/session-store.ts"
      provides: "扩展后的 timeline item 类型定义与构造辅助"
      min_lines: 30
    - path: "src/components/Timeline.tsx"
      provides: "按 item 类型渲染的统一时间线组件"
      min_lines: 40
  key_links:
    - from: "src/app.tsx"
      to: "src/state/session-store.ts"
      via: "timeline updates"
      pattern: "timeline|status|error|complete"
---

<objective>
统一时间线 item 模型并落地到渲染层。Purpose: 先把数据结构和渲染骨架搭稳，后续错误块、完成态和焦点恢复才能落在统一通道里。Output: 新的 timeline item 类型、Timeline 渲染分发逻辑，以及围绕统一时间线的基础测试。
</objective>

<execution_context>
@.planning/phases/03-timeline-and-reliability/03-CONTEXT.md
@.planning/phases/03-timeline-and-reliability/03-RESEARCH.md
@.planning/phases/03-timeline-and-reliability/03-VALIDATION.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 timeline item 结构</name>
  <files>src/state/session-store.ts, src/app.tsx</files>
  <action>把当前 `TimelineEntry` 扩展为 discriminated union 或等价结构，至少覆盖用户消息、助手消息、状态事件、错误块与完成事件；同时补充构造辅助函数，避免 `App` 继续直接拼裸对象。</action>
  <verify>检查 `src/state/session-store.ts` 中存在明确的 item 类型区分</verify>
  <done>时间线数据结构足以承载 Phase 3 的所有渲染语义</done>
</task>

<task type="auto">
  <name>Task 2: 让 Timeline 组件按类型渲染</name>
  <files>src/components/Timeline.tsx, tests/components/timeline.test.tsx</files>
  <action>重构 `Timeline` 组件，使其按 item 类型渲染不同前缀、边框或说明文案，并补充组件级测试验证各种 item 至少能稳定落盘。</action>
  <verify>npm test -- tests/components/timeline.test.tsx</verify>
  <done>Timeline 不再只会渲染一类纯文本消息</done>
</task>

<task type="auto">
  <name>Task 3: 把命令反馈接入统一时间线模型</name>
  <files>src/app.tsx, tests/components/app-timeline-events.test.tsx</files>
  <action>把 `/model` 等现有命令反馈接到统一 timeline item 写入路径，确保命令结果与普通会话事件遵守同一输出协议。</action>
  <verify>npm test -- tests/components/app-timeline-events.test.tsx</verify>
  <done>命令反馈与普通交互共享统一时间线模型</done>
</task>

</tasks>

<verification>
- [ ] `Timeline` 已能区分多种 item 类型
- [ ] 命令反馈进入统一时间线
- [ ] `npm test -- tests/components/timeline.test.tsx tests/components/app-timeline-events.test.tsx` 可通过
</verification>

<success_criteria>
- 统一时间线 item 模型已落地
- 时间线渲染骨架稳定
- 后续错误块与完成态有明确承载层
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-and-reliability/03-01-SUMMARY.md`
</output>
